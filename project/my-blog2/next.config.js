/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    trailingSlash: true,
    env: {
        SANITY_PROJECT_ID: "jt5i56bz",
    },
};

module.exports = nextConfig;
